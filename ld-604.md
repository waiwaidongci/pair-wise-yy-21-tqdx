# ld-604 电力配网抢修工单系统

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**电力配网抢修工单系统（grid-repair）**：面向供电所的配网故障报修、抢修派工、备件领用和停电恢复跟踪平台。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

1. **配网资产（GridAsset）**
   - 字段：id, asset_code, asset_type, feeder_line, voltage_level, location_desc, health_status, owner_team_id
   - 关键关系：被故障工单和巡检记录引用
   - 业务动作：资产台账、健康状态、线路归属
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

2. **故障报修（FaultReport）**
   - 字段：id, reporter_name, phone, asset_id, fault_type, address_desc, severity, report_channel, status
   - 关键关系：可生成 RepairTicket 并关联受影响用户
   - 业务动作：报修登记、故障分级、重复合并
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

3. **抢修工单（RepairTicket）**
   - 字段：id, fault_report_id, team_id, dispatcher_id, priority, status, assigned_at, restored_at
   - 关键关系：关联抢修队和备件领用
   - 业务动作：派工、到场、处理、复电确认
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

4. **抢修班组（Crew）**
   - 字段：id, name, leader_id, skill_tags, duty_status, current_ticket_id, contact_phone
   - 关键关系：承接 RepairTicket
   - 业务动作：值班维护、任务分派、状态切换
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

5. **备件领用（SparePartUsage）**
   - 字段：id, ticket_id, part_code, part_name, quantity, warehouse_name, approved_by, usage_status
   - 关键关系：随工单出库并形成库存日志
   - 业务动作：申请备件、审批、归还/消耗
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

1. **抢修态势** `/dashboard`
   - 消费实体/模型：FaultReport + RepairTicket + Crew
   - 关键交互：待派工数量、抢修进度、班组状态、平均复电时间
   - 共享组件：StatCard, StatusBadge, PriorityTag

2. **配网资产** `/assets`
   - 消费实体/模型：GridAsset + FaultReport
   - 关键交互：资产列表、线路筛选、健康状态、历史故障
   - 共享组件：StatusBadge, AssetTree, FilterBar

3. **故障报修** `/faults`
   - 消费实体/模型：FaultReport + GridAsset + RepairTicket
   - 关键交互：登记报修、合并重复、故障分级、生成工单
   - 共享组件：PriorityTag, StatusBadge, TimelineList

4. **抢修工单** `/tickets`
   - 消费实体/模型：RepairTicket + Crew + SparePartUsage
   - 关键交互：派工、状态流转、备件申请、复电确认
   - 共享组件：StatusBadge, TimelineList, CrewCard

5. **备件领用** `/parts`
   - 消费实体/模型：SparePartUsage + RepairTicket
   - 关键交互：备件申请、审批、使用记录和消耗统计
   - 共享组件：StatusBadge, ApprovalPanel, EmptyState

### 4. 横切关注点（≥ 2 个）

1. **RBAC 权限（调度员/班组长/仓管/审计员）**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。
2. **操作日志 + 备件库存流水**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。
3. **全局异常处理 + 接口限流**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。

### 5. 共享枚举/常量（≥ 2 组）

1. **FaultType**：OUTAGE / VOLTAGE_LOW / TRIP / EQUIPMENT_DAMAGE / SAFETY_RISK
   - 前端位置：`frontend/src/constants/FaultType.ts` 或同名模块
   - 后端位置：`backend/src/constants/FaultType.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
2. **TicketStatus**：WAIT_DISPATCH / ASSIGNED / ARRIVED / REPAIRING / RESTORED / CLOSED
   - 前端位置：`frontend/src/constants/TicketStatus.ts` 或同名模块
   - 后端位置：`backend/src/constants/TicketStatus.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
3. **AssetHealthStatus**：NORMAL / WATCH / DEGRADED / DANGEROUS
   - 前端位置：`frontend/src/constants/AssetHealthStatus.ts` 或同名模块
   - 后端位置：`backend/src/constants/AssetHealthStatus.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置

### 6. 共享前端组件清单（≥ 3 个）

- `StatusBadge`
- `PriorityTag`
- `AssetTree`
- `CrewCard`
- `TimelineList`

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useTicketFlow`
- `useCrewAvailability`
- `usePagination`

### 8. 后端中间件清单（≥ 2 个）

- `authMiddleware`
- `rbacMiddleware`
- `auditLogMiddleware`
- `errorHandlerMiddleware`
- `requestLoggerMiddleware`

### 9. 技术要求

- 英文短名（用于 Docker Compose）：`grid-repair`
- 前端端口：`20104`
- 后端端口：`21104`
- 后端容器内部端口：`3000`
- 前端技术栈：Vue 3 + TypeScript + Vite + Element Plus + Pinia
- 后端技术栈：Node.js + Express + TypeScript + Prisma
- 数据库：MySQL 8.0
- 认证方式：JWT + RBAC
- 数据来源：全部本地数据库和种子数据，禁止接入第三方 API

### 复杂度下限与耦合目标

本项目用于验证跨文件协同改动能力，禁止将多个职责合并进单一文件。即使是简单功能，也必须按照分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。

必须刻意形成“低内聚、高耦合”的企业级屎山结构：
1. 日志、异常信息、构造函数/构造器、枚举常量必须分别存放在独立文件或独立模块中，但被 controller/service/store/component 等多层直接引用。
2. 日志模板集中在 `constants/logTemplates.*`，每个实体至少 4 条日志模板，所有写操作都要记录日志；字段变更时必须同步改日志模板和调用处。
3. 错误码集中在 `constants/errorCodes.*`，错误消息模板集中在 `constants/errorMessages.*`，但 service 和 controller 必须分别包装异常，禁止只在一个全局位置吞掉全部异常。
4. 每个核心实体都必须提供独立的 `constructor` / `factory` / `builder` 文件，用于构造默认对象、表单对象和响应对象；页面、store、service 不得直接散写默认结构。
5. 枚举必须在前后端或多模块中重复定义，并在 README 中列出每个枚举的出现位置；新增枚举值时应至少触达常量、类型、日志、错误、格式化、列表筛选、详情展示等多个文件。
6. `utils/formatters.*` 可以故意混合日期、金额、状态文本、风险等级等格式化逻辑，让多个页面和服务共同依赖，形成牵一发动全身的修改面。
7. 全局配置必须分散经过 `.env.example`、`docker-compose.yml`、`config/*`、请求封装、日志模块读取；新增配置必须同步多处。


### 文件结构强制清单

前端必须包含：
```text
frontend/src/
├── api/                  # 按实体分文件，每个实体一个 .ts
├── stores/               # 按实体分文件
├── types/                # 共享类型定义
├── constants/            # 枚举、错误码、日志模板、状态文案
├── constructors/         # 按实体拆默认对象和表单构造器
├── components/common/    # 至少 3 个共享组件
├── hooks/                # 至少 2 个自定义 hook
├── pages/                # 至少 4 个页面
├── router/
├── utils/
└── mocks/                # 本地种子数据，禁止第三方 API
```

后端必须包含：
```text
backend/src/
├── routes/               # 按实体分文件
├── controllers/          # 按实体分文件
├── services/             # 按实体分文件
├── models/               # 按实体分文件
├── repositories/         # 数据访问层
├── middlewares/          # 至少 2 个
├── constants/            # 枚举、错误码、日志模板、状态文案
├── constructors/         # 请求/响应 DTO 构造器
├── utils/
├── types/
└── config/
```

严禁合并职责到单一文件。


### 部署要求

这是前后端分离项目，必须支持 Docker Compose 一键部署，且在任意目录名（包括中文目录名）下都能正常启动。

必须提供：
- 根目录 `docker-compose.yml`，编排 `frontend`、`backend`、`db`，不要写 `version:` 字段
- 根目录 `.env`，包含 `COMPOSE_PROJECT_NAME=grid-repair`、`FRONTEND_PORT=20104`、`BACKEND_PORT=21104`、`DB_PORT`
- 根目录 `.env.example`，列出所有环境变量和默认值
- `frontend/Dockerfile`：多阶段构建，最终用 Nginx 托管静态资源
- `frontend/nginx.conf`：`location /api/` 反向代理到 `http://backend:3000/`，并配置 `try_files $uri $uri/ /index.html;`
- `backend/Dockerfile`
- `database/init.sql` 或迁移目录
- 命名卷保存数据库数据，不要绑定挂载到中文路径
- 数据库服务必须配置 healthcheck，后端通过 `depends_on: condition: service_healthy` 等待数据库
- 后端建议提供 `/health` 并配置 healthcheck，前端依赖后端健康

`docker-compose.yml` 必须满足：
- 顶层 `name: grid-repair`
- 所有服务使用 `container_name`，命名带 `${COMPOSE_PROJECT_NAME:-grid-repair}` 前缀
- 前端端口映射：`${FRONTEND_PORT:-20104}:80`
- 后端端口映射：`${BACKEND_PORT:-21104}:3000`
- 前端代码里统一请求 `/api`，禁止硬编码 `localhost`

启动后要求：
- `docker compose config --quiet` 不报错
- `docker compose up -d` 后前端访问 `http://localhost:20104`
- 后端健康检查访问 `http://localhost:21104/health`
- README 首选启动命令必须是 `cp .env.example .env && docker compose up -d`


### README 写法

根目录必须有中文 `README.md`，且 Docker 启动方式放在最前面。README 必须包含：
- 项目名称和一句话介绍
- 快速启动方式
- 本地开发方式
- 访问地址或 CLI 示例
- 技术栈表格
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、命名卷、常见问题）
- 枚举/常量出现位置清单
- 为什么该项目会牵一发动全身的说明
- License

### Git 要求

代码编写完成后初始化 Git，并创建 `.gitignore`，至少包含：
```text
node_modules/
dist/
build/
.env
.DS_Store
*.log
.idea/
.vscode/
coverage/
__pycache__/
```
